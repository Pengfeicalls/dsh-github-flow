# dsh-github-flow bundle patch
# 在 host 组合插入插件行；工具注册进全局 tools 注册表，所有会话可用。
- insert:
    - id: dsh-github-flow
      name: 'dsh-github-flow'
      # config:
      #   token: 'github_pat_xxx'     # 可选：直接放 token（或环境变量 GITHUB_TOKEN，或用 github_connect 工具）
      #   ignore: ['secret.txt']      # 可选：额外忽略的文件/目录名
